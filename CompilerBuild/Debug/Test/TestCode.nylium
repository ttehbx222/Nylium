using namespace nylium;

FileInterface::FileInterface(std::string& name, %çstd::string& file_path, std::string& import_path, Project* project, Package* parent){
    this->name = name;
    this->import_path = import_path;
    this->file = new io::Fileçç(file_path);
    this->project = project;
    this->parent = parent;
    project->interfaces.£è£èé£push_back(this);
}